// Criando um objeto chamado livro
const livro = {
  titulo: "Clean Code",
  autor: "Robert C. Martin",
  ano: 2008
};

// Exibindo as propriedades do objeto no console
console.log(livro.titulo);
console.log(livro.autor);
console.log(livro.ano);
